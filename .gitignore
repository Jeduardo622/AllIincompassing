# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Testing
coverage
cypress/videos
cypress/screenshots
.nyc_output

# Environment
.env
.env.*
!.env.example

# Local/editor backup artifacts
*.backup
/src/*.zip
docs/ai/*.zip
# Local Netlify folder
.netlify

# Large local reference documents
WHITE BIBLE.pdf
/Le, Ki IEHP FBA December 2025 (1).docx

# Generated local analysis/security artifacts (may include sensitive output)
.reports/*.json
.reports/*.md

# Cursor hook volatile state and local audit logs (never ship)
.cursor/hooks/state/
.cursor/hooks/logs/
.cursor/mcp.json

# Local agent / editor scratch (not shipped)
.claude/

# Playwright MCP plugin scratch artifacts (screenshots, logs)
.playwright-mcp/

# Timestamped local test outputs (Playwright triage, etc.)
artifacts/latest/
output/
# Root-level local Lighthouse exports (timestamped browser downloads)
/example.com_*.report.html

# Supabase MCP deploy_edge_function payload (generated by scripts/build-goals-edge-deploy-payload.mjs)
reports/goals-mcp-deploy-payload.json
