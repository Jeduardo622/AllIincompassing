# Placeholder diff for therapist schedule protections
@@
- const filters: SessionFilters = { therapist_id: url.searchParams.get('therapist_id') || undefined, ... };
- let query = db.from('sessions').select(...);
- if (filters.therapist_id) query = query.eq('therapist_id', filters.therapist_id);
+const callerOrg = await resolveCallerOrganization(userContext);
+const filters: SessionFilters = normalizeFilters(url.searchParams);
+let query = db.from('sessions').select(...).eq('organization_id', callerOrg);
+if (!filters.therapist_id && userContext.profile.role === 'therapist') {
+  query = query.eq('therapist_id', userContext.profile.therapist_id);
+}
